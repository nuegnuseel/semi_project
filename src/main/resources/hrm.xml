<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrproject.hrproject.dao.HrmDao">


    <sql id="search">
        <choose>
            <when test="search == 'empno'">
                empno LIKE CONCAT('%', #{searchWord}, '%')
            </when>
            <when test="search == 'ename'">
                ename LIKE CONCAT('%', #{searchWord}, '%')
            </when>
            <when test="search == 'deptname'">
                deptname LIKE CONCAT('%', #{searchWord}, '%')
            </when>
            <when test="search == 'email'">
                email LIKE CONCAT('%', #{searchWord}, '%')
            </when>
            <otherwise>
                empno LIKE CONCAT('%', #{searchWord}, '%') OR
                ename LIKE CONCAT('%', #{searchWord}, '%') OR
                deptname LIKE CONCAT('%', #{searchWord}, '%') OR
                email LIKE CONCAT('%', #{searchWord}, '%')
            </otherwise>
        </choose>
    </sql>

    <select id="getHrmTotal" resultType="int">
        SELECT COUNT(*) AS TOTAL FROM HRM WHERE showAble = 1
    </select>

    <select id="getSearchHrmTotal" resultType="int" parameterType="HashMap">
        select count(*) as total from HRM WHERE showAble = 1
        <if test="search != null and searchWord != null">
            and <include refid="search"></include>
        </if>
    </select>

    <select id="getHrm" resultType="HrmDto" parameterType="HrmDto">
        SELECT * FROM HRM WHERE EMPNO = ${empNo} AND showAble = 1
    </select>

    <select id="getHrmList" resultType="HrmDto" parameterType="HrmPageDto">
        SELECT * FROM (
        SELECT *, ROW_NUMBER() OVER () AS num FROM HRM WHERE showAble = 1
        ) AS numbered_rows
        LIMIT #{start}, #{end}
    </select>

    <select id="getSearchHrmList" resultType="HrmDto" parameterType="HrmPageDto">
        SELECT * FROM (
        SELECT *, ROW_NUMBER() OVER () AS num FROM HRM WHERE showAble = 1
        ) AS numbered_rows where
        <if test="search != null and searchWord != null">
            <include refid="search"></include>
        </if>
        LIMIT #{start}, #{end};
    </select>

    <insert id="insertHrm" parameterType="HrmDto">
        INSERT INTO HRM (
        EMPNO, ENAME, FOREIGN_NAME, DEPTNO, DEPTNAME, POSITION, MOBILE, PASSPORT, EMAIL, HIREDATE, HIRETYPE, RESIGNDATE,
        OUTREASONS, BANK_NAME, ACCOUNT, ACCOUNT_HOLDER, POSTCODE, ADDRESS, ADDRESS_DETAIL, ORIGINAL_PROFILE, RENAME_PROFILE,
        ETC_FILE, REMARKS, POSITION_NO, ROLE, SHOWABLE
        ) VALUES (
        #{empNo}, #{ename}, #{foreignName}, #{deptNo}, #{deptName}, #{position}, #{mobile}, #{passport}, #{email},
        #{hireDate}, #{hireType}, #{resignDate}, #{outReasons}, #{bankName}, #{account}, #{accountHolder}, #{postCode},
        #{address}, #{addressDetail}, #{originalProfile}, #{renameProfile}, #{etcFile}, #{remarks}, #{positionNo}, #{role}, 1
        )
    </insert>

    <update id="updateHrm" parameterType="HrmDto">
        UPDATE HRM
        SET
        EMPNO = #{empNo},
        ENAME = #{ename},
        FOREIGN_NAME = #{foreignName},
        DEPTNO = #{deptNo},
        DEPTNAME = #{deptName},
        POSITION = #{position},
        MOBILE = #{mobile},
        PASSPORT = #{passport},
        EMAIL = #{email},
        HIREDATE = #{hireDate},
        HIRETYPE = #{hireType},
        RESIGNDATE = #{resignDate},
        OUTREASONS = #{outReasons},
        BANK_NAME = #{bankName},
        ACCOUNT = #{account},
        ACCOUNT_HOLDER = #{accountHolder},
        POSTCODE = #{postCode},
        ADDRESS = #{address},
        ADDRESS_DETAIL = #{addressDetail},
        ORIGINAL_Profile = #{originalProfile},
        RENAME_Profile = #{renameProfile},
        ETC_FILE = #{etcFile},
        REMARKS = #{remarks},
        POSITION_NO = #{positionNo},
        ROLE = #{role}
        WHERE EMPNO = #{oldEmpNo}
    </update>

    <update id="setShowAbleHrm">
        UPDATE HRM SET SHOWABLE = 0 WHERE EMPNO IN
        <foreach collection="array" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </update>
</mapper>