<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrproject.hrproject.mybatis">
    <insert id="insertSalary" parameterType="SalaryDto">
        INSERT INTO SALARY (EMPNO, ACCOUNTINGPERIOD, SALARYCATEGORY, SALARYNAME, SALARYDAY, SALARY, SALARYINFO, BONUS)
        VALUES (
        #{empNo},
        #{accountingPeriod},
        #{salaryCategory},
        #{salaryName},
        #{salaryDay},
        #{salary},
        #{salaryInfo},
        #{bonus}
        )
    </insert>

    <select id="getSalaryList" resultType="SalaryDto">
        select * from salary
    </select>

</mapper>